version: '3.8'

services:
  markov-feed:
    restart: unless-stopped
    build:
      context: .
      args:
        TZ: Asia/Tokyo
        USERS: iam_takagi
        TWITTER_CK: xxx
        TWITTER_CS: xxx
        TWITTER_AT: xxx
        TWITTER_ATS: xxx
        DISCORD_WEBHOOK_URL: https://discord.com/api/webhooks/xxx/xxx
